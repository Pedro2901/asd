<div class="row">
    {{#each productos}}
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{name}} 
                </h4>
                <p>el precio es {{price}}</p>
                <p>Su categoria es {{category}}</p>
                 
                <form action="/order/add/{{_id}}" method="POST">
                 <div class="mb-3">
                        <input type="Number" class="form-control" name="quantity" placeholder="quantity" value=1>
                    </div>
                <button class="btn btn-danger btn-block btn-sm" type="submit">Añadir al carrito</button>
                </form>
            </div>
        </div>
    </div>
    {{else  }}
    <div class="card mx-auto">
        <div class="card-body">
            
            <p class="lead">no hay pedidos </p>
            
        </div>
    </div>
    {{/each}}
</div>